# 使用与商标说明

RR Edge Hunter Android（应用名：CF 优选IP）是非官方独立项目，仅用于在用户自有或获明确授权的 Argo 节点和网络上，对 Cloudflare 官方网段入口进行网络质量评估与选择。结果 IP 用于节点的 `address/server` 字段，原节点域名继续承担 TLS SNI 与 HTTP Host。

本项目不提供任意强制路由、`hosts` 修改、任何 DNS 记录写入、代理、端口扫描、漏洞探测、压力测试、绕过访问控制或网络访问服务。当前 DNS 用于确认 Argo 域名和提供种子；其他候选只允许来自 Cloudflare 官方 CIDR，并受到候选量、并发、流量、证书、SNI/Host 和真实远端校验约束。

使用者应自行确认测试主机、网络和流量已获得所需授权，并遵守所在地法律、网络提供商政策及相关服务条款。

Cloudflare、Android 及其他名称和商标归各自权利人所有。项目中出现这些名称仅用于说明兼容性、公开测试端点或技术实现。本项目与相关权利人不存在隶属、合作、赞助、认证或背书关系。

测试结果只代表当前设备、当前网络出口与当前时间。本项目不承诺结果适用于其他设备、网络或时段，也不对不当使用造成的损失承担责任。
